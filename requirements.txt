# Runtime dependencies only (same set as [project.dependencies] in pyproject.toml).
# Installs the libraries Waveshift needs, but not the package itself.
#   pip install -r requirements.txt
numpy>=1.20
scipy>=1.7
pillow>=8.0
